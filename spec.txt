lots of detail

a user, on a webpage with a javascript front end
    adjusts points on a timeline with values
    clicks the GO button

IN SET MODE
	A user moves an onscreen slider left to right to move faster or slower in a direction
	the user presses the save keyframe button which stores the state of all motors at that time

IN PLAYBACK MODE


The
The Teensy 3.1 sends control information to:
    the stepper driver,
        which currently is a schmalzhaus easydriver
    and the servo driver,
            which is a 16 channel servo driver from adafruit,
    both send the appropriate values to the motors.

and the camera moves
